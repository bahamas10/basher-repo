127.0.0.1	localhost
255.255.255.255	broadcasthost
::1             localhost
fe80::1%lo0	localhost

<%
	ENV['DATA'].strip.split("\n").each do |line|
		name, ip = line.split()
-%>
<%= "#{name}\t#{ip}" %>
% end
